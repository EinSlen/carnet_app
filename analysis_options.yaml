include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # Désactivé : les patterns async + Riverpod déclenchent beaucoup de faux
    # positifs ici (les usages de context sont protégés par des gardes `mounted`).
    use_build_context_synchronously: false
